# Cycle: DISCOVERED バッチ改善 (#33, #34, #35)

- Issue: #33, #34, #35
- phase: DONE
- Created: 2026-02-17

## Goal

Issue #29 の quality-gate で発見された3件の改善を一括実施する。

## Context

- #33: TC-REF-13/14/15 の grep パターンをセクション内検索に改善 (correctness)
- #34: test script の I/O 重複最適化 (performance)
- #35: reference.md に Table of Contents 追加 (usability)
- 対象: tests/test-security-audit-reference.sh, skills/security-audit/reference.md

## Acceptance Criteria

- [ ] TC-REF-13/14/15 がセクション内検索に変更されている
- [ ] テストスクリプトがファイルを1回読み込みで再利用している
- [x] ~~reference.md 冒頭に Table of Contents がある~~ → REJECTED (quality-gate: 他reference.mdと不整合、AI読者にTOC不要)
- [ ] 既存17テストが全て PASS

## Environment

- Project: dev-crew (Claude Code Plugin)
- Test Framework: Bash test scripts
- Target Files:
  - tests/test-security-audit-reference.sh (178 lines)
  - skills/security-audit/reference.md (178 lines)

## PLAN

### 設計方針

3つの独立した改善を同一ファイル群に適用:

1. **Correctness (#33)**: grep パターンをセクション内検索に改善
   - TC-REF-13/14/15 が誤判定する可能性を排除
   - ファイル全体検索 → Error Handlingセクション内検索
   - パターン: `grep -A N "^## Error Handling" | grep -q "### [skill] 失敗時"`

2. **Performance (#34)**: I/O重複を1回読み込み+再利用に変更
   - 178行ファイルを17回読み込み → 1回読み込み+変数再利用
   - `CONTENT=$(cat "$REFERENCE_FILE")` → `echo "$CONTENT" | grep`

3. **Usability (#35)**: reference.md にナビゲーション支援を追加
   - 178行のドキュメントに TOC を追加（タイトル直後）
   - Markdown anchor リンク形式（`#overview`, `#workflow-details` など）

### ファイル構成

**変更対象**:
- `tests/test-security-audit-reference.sh` (#33, #34)
- `skills/security-audit/reference.md` (#35)

**変更なし**:
- 他のテストスクリプト（独立している）

### 依存関係

- #33 と #34 は同一ファイル変更（競合なし、順序自由）
- #35 は独立（reference.md のみ）
- 既存17テスト（TC-REF-01~17）の PASS が前提条件

### 品質基準

- 既存17テストが全て PASS
- 新規テスト（TC-REF-18, TC-REF-19）も PASS
- シェルスクリプト文法エラーなし（`bash -n`）

## Test List

### TODO

#### #33: セクション内検索 (Correctness)

- [ ] TC-REF-13: Error Handling セクション内で "### security-scan 失敗時" を検出
- [ ] TC-REF-14: Error Handling セクション内で "### attack-report 失敗時" を検出
- [ ] TC-REF-15: Error Handling セクション内で "### generate-e2e 失敗時" を検出

#### #34: I/O最適化 (Performance)

- [ ] TC-REF-18: テストスクリプトが CONTENT 変数で1回読み込みを実行
- [ ] TC-REF-19: 全テストが CONTENT 変数を再利用（`echo "$CONTENT" | grep`）

#### #35: TOC追加 (Usability) → REJECTED

quality-gate usability-reviewer: 他の reference.md (init 389行, orchestrate 246行) は全て TOC なし。
1ファイルのみ追加は一貫性を破る。主な読者はAIエージェント（コンテキスト注入）で TOC の恩恵がない。

#### 既存テスト保証

- [ ] TC-REF-01~17: 全て PASS（回帰テスト）

## DISCOVERED

(none)
