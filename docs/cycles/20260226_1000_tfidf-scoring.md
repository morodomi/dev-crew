# Cycle: TF-IDF + COUNT スコアリング

- Phase: DONE
- Created: 2026-02-26

## Goal

observer/learn のスコアリングを TF-IDF + COUNT 方式に変更し、全パターンが同一 confidence に収束する問題を解決する。

## Changes

| File | Change |
|------|--------|
| `agents/observer.md` | 信頼度テーブルを 2軸 (TF-IDF + COUNT) に変更 |
| `skills/learn/SKILL.md` | Step 2 に TF-IDF サマリ計算を追加 |
| `skills/learn/reference.md` | TF-IDF 仕様、Term 定義、2軸評価式を追記 |

## Test List

- TC-06: observer.md に TF-IDF ベースの信頼度テーブルがある
- TC-07: observer.md に evidence_multiplier テーブルがある
- TC-08: observer.md に 2軸計算式 (tfidf_to_base * evidence_multiplier) がある
- TC-09: observer.md Input に tfidf_summary フィールドがある
- TC-10: learn SKILL.md Step 2 に TF-IDF サマリ計算がある
- TC-11: learn reference.md に TF-IDF 計算式 (TF, IDF, TF-IDF) がある
- TC-12: learn reference.md に ブートストラップ閾値 (セッション数 < 20) がある
- TC-13: learn reference.md に Term 定義がある
- TC-14: observer.md に TF-IDF < 0.2 破棄ルールがある
- TC-15: observer.md に COUNT < 3 破棄ルールがある

## RED Phase

Tests written: test-tfidf-scoring.sh (10 tests, all initially failing)

## GREEN Phase

- `agents/observer.md`: Replaced count-only confidence table with 2-axis (tfidf_to_base * evidence_multiplier). Added tfidf_summary input field. Kept fallback table for bootstrap period.
- `skills/learn/SKILL.md`: Added TF-IDF summary computation sub-steps in Step 2. Bootstrap threshold (sessions < 20).
- `skills/learn/reference.md`: Added Term definition, TF/IDF/TF-IDF formulas, bootstrap specification, tfidf_summary output format.

## REVIEW Phase

- All 10 new tests pass
- All 8 existing learn/evolve tests pass (zero regressions)
- SKILL.md: 96 lines (< 100 limit)
- observe.sh, evolve, hooks.json: unchanged (as planned)

## Follow-up: tfidf-summary.sh script

### Changes

| File | Change |
|------|--------|
| `scripts/tfidf-summary.sh` | jq+awk パイプラインで TF-IDF 計算 (新規) |
| `skills/learn/SKILL.md` | Step 2 に `bash scripts/tfidf-summary.sh` 呼び出しを追記 |
| `tests/test-tfidf-summary-script.sh` | 単体テスト 6件 (新規) |

### Test List

- TC-16: スクリプトが存在し実行可能
- TC-17: 空 log.jsonl → 空配列 `[]`
- TC-18: セッション数 < 20 → 空配列 (ブートストラップ)
- TC-19: 正常データ → 有効な JSON 配列
- TC-20: 必須フィールド (term, tf, idf, tfidf, count, sessions)
- TC-21: learn SKILL.md が tfidf-summary.sh を参照

### Results

- 6/6 new tests pass
- 15/15 existing tests pass (zero regressions)
- Real data: 90 terms from 306 sessions, valid JSON output
